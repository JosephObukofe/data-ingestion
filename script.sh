# Investigating the tag to remove the docker container automatically before it exits → --rm
docker run --rm

# Investigating the version of the package 'wheel' → 0.42.0
docker run -it --entrypoint bash python:3.9